#include <iostream>

using namespace std;

int main()
{
	int N, res=666, tmp=1000;
	cin >> N;

	for (int i = 1; i < N; i++) {
		int tmp1 = res*tmp;

		while (1) {
			if (tmp1)
		}
	}

	cout << res << endl;
}
